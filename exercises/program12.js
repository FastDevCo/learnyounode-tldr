// lib = require libname

// port from args
// file from args

//server = create server
  // request has a property called `method` that can be GET, POST...
  // request has a property called `pipe` that pipes it as a stream to somewhere
  // req.pipe(...).pipe(res) to pipe to the response
  // string has a method for uppercasing


// server listen port
